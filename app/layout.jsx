import "./globals.css";

export const metadata = {
  title: "BONUS FUN",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>
        {children}
      </body>
    </html>
  );
}
